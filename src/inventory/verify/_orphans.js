module.exports = function _orphans(inventory, callback) {

  // look for lambdas starting with inventory.app
  // look for sns topics starting with inventory.app
  // look for dynamodb tables starting with
  // look for cloudwatch events/rules starging with inventory.app
  callback()
}
